NODE_ENV=production
PORT=5000
HTTPS_PORT=5443
USE_HTTPS=true

# Frontend build configuration
VITE_BASE_URL=/
VITE_API_URL=http://10.10.0.16:5000

# Point to the mysql container name on the same Docker network
MYSQL_HOST=mysql-server
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=StrongPassword123
MYSQL_DATABASE=ponghospital
MYSQL_SSL=false

# App settings
JWT_SECRET=change-me-to-a-long-random-value
ALLOWED_ORIGINS=http://10.10.0.16:5000,http://localhost:5000,http://127.0.0.1:5000
LOG_DB=1
